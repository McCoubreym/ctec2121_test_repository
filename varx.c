#include <stdio.h>

int main()
{
    int x = 30;
    int y = 5;

    printf ("%i, this is the value of x\n", x+y);
    printf ("%i, this is the value of x\n", x-y);
    printf ("%i, this is the value of x\n", x*y);
    printf ("%i, this is the value of x\n", x/y);
    return 0;
}
